This branch is initial prototyping in numpy for soft versions of minimum and maximum operators for CBF-DDP. The `smoothmax_2d` branch has the most recent updates with all results that were shared.

To run the code,

- `python evaluation.py`
- `python evaluation_2D.py`
- `python evaluation_bic4D.py`
- `python evaluation_bic5D.py`
